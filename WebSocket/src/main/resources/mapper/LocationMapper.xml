<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.websocketserver.mapper.LocationMapper">
    <insert id="insertLocation">
        INSERT INTO location (
            latitude, longitude,record_key
        ) VALUES (
                #{latitude}, #{longitude},#{recordKey}
                 )
    </insert>


</mapper>